[project]
name = "onetsp-importer"
version = "0.8.0"
description = "Code to take OneTsp backup files and import to other recipe apps."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pyyaml",
  "requests"
]

[project.scripts]
recipes-cli = "cli:main"
